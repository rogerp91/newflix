version: 2
jobs:
  coverage:
    working_directory: ~/Newflix
    docker:
      - image: circleci/android:api-29
    environment:
      GRADLE_OPTS: -Xmx4g -Dorg.gradle.daemon=false
      JVM_OPTS: -Xmx4g
    steps:
      - checkout
      - run:
          name: Send coverage
          command: bash <(curl -s https://codecov.io/bash) -t e61b4cb8-f9c9-4a19-babd-23237e0ef833
  danger:
    working_directory: ~/Newflix
    docker:
      - image: circleci/android:api-29
    environment:
      GRADLE_OPTS: -Xmx4g -Dorg.gradle.daemon=false
      JVM_OPTS: -Xmx4g
    steps:
      - checkout
      - run:
          name: Danger
          command: bundle exec danger --verbose
deployment:
  coverage:
    branch: [master]
    commands:
      - bash <(curl -s https://codecov.io/bash) -t e61b4cb8-f9c9-4a19-babd-23237e0ef833
workflows:
  version: 2
  build_and_test:
    jobs:
      - coverage
